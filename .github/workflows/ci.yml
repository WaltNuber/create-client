name: CI

on:
  pull_request:
  release:
      types: [created]

jobs:
  build:
    strategy:
      matrix:
        node-version: ['lts/*', 'current']

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm run build
    - run: npm test
    - run: npm run lint
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - run: npm run test-gen
    - run: npm run test-next-app
    - run: npm run test-react-app
    - run: npm run test-vue-app
    - env:
        NODE_OPTIONS: --openssl-legacy-provider
      run: npm run test-nuxt-app
    - run: npm run test-gen-env
    - run: npm run test-gen-openapi3
    - run: npm run eslint-check
    - uses: actions/upload-artifact@v3
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
